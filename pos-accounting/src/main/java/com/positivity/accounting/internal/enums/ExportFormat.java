package com.positivity.accounting.internal.enums;

/**
 * Export format for financial reports.
 */
public enum ExportFormat {
    /** PDF format - professional formatted document */
    PDF,

    /** CSV format - raw tabular data */
    CSV,

    /** XLSX format - Excel workbook (v2.0 future enhancement) */
    XLSX,

    /** JSON format - machine-readable (v2.0 future enhancement) */
    JSON
}
