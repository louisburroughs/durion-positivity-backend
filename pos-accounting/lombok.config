config.stopBubbling = true
lombok.addLombokGeneratedAnnotation = true
# stopBubbling above blocks inheritance from the root lombok.config, so the
# Jackson-3 setting for @Jacksonized must be repeated here or it silently
# reverts to generating Jackson 2 annotations that Spring Boot 4 ignores.
lombok.jacksonized.jacksonVersion += 3
