openapi: "3.0.3"
info:
  title: pos-accounting
  version: "1.0"
paths:
  /v1/accounting:
    get:
      summary: List accounting entries
      description: Returns a paginated list of accounting entries.
      responses:
        "200":
          description: OK
