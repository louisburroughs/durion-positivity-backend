spring:
  cloud:
    gateway:
      routes:
        - id: inventory-deactivate-location
          uri: lb://pos-inventory
          predicates:
            - Path=/v1/inventory/locations/*/deactivate
            - Method=POST
          filters:
            - RemoveRequestHeader=Cookie
