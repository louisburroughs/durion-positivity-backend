spring:
  application:
    name: pos-customer-test
  datasource:
    # MODE=PostgreSQL restored. This profile file shadows application.yml in the same directory,
    # which sets it; dropping the mode here silently put every test on the "test" profile onto plain
    # H2 dialect while the base config, this module's dev profile and production are all Postgres.
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: false
  h2:
    console:
      enabled: false
  main:
    allow-bean-definition-overriding: true

# Disable security for tests
security:
  enabled: false

# Disable eureka for tests  
eureka:
  client:
    enabled: false


# Logging
logging:
  level:
    com:
      positivity: DEBUG
    org:
      springframework:
        security: DEBUG
