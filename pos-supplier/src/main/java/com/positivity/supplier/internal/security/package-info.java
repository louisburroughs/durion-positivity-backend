/**
 * Permission constants gating the module's controllers (ADR-0025, ADR-0040). Names mirror
 * {@code permissions.yaml} and the platform permission catalogs exactly.
 */
package com.positivity.supplier.internal.security;
