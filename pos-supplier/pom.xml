<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.positivity</groupId>
        <artifactId>positivity</artifactId>
        <version>${revision}</version>
    </parent>

    
    <artifactId>pos-supplier</artifactId>
    <name>pos-supplier</name>
    <description>POS Supplier integration module (outbound supplier connectivity, ADR-0049..0052)</description>

    <properties>
        <!-- Coverage ratchet: measured 87.1% line / 74.8% branch by the gate's own
             command, `verify -DskipITs` (docs/TEST_COVERAGE_IMPROVEMENT_PLAN.md §6.1).
             Floors sit 3 points under. Re-derive them ONLY from a -DskipITs run — an
             IT-inclusive measurement describes coverage the gate cannot see. -->
        <jacoco.line.min>0.84</jacoco.line.min>
        <jacoco.branch.min>0.71</jacoco.branch.min>
    </properties>
    <packaging>jar</packaging>
    <dependencies>
        <!-- Lombok for entity boilerplate (warranty entity convention) -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>provided</scope>
        </dependency>
        <!-- SLF4J API for logging -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>
        <!-- POS Security Common: audit actor sourcing from the security context (ADR-0018) -->
        <dependency>
            <groupId>com.positivity</groupId>
            <artifactId>pos-security-common</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- POS Events for @EmitEvent audit instrumentation + event-type registration (AGENTS.md) -->
        <dependency>
            <groupId>com.positivity</groupId>
            <artifactId>pos-events</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!--
            CAP-319 (#1225): the live stock inquiry's short-TTL cache. Caffeine rather than a
            hand-rolled map because this cache is on a customer-facing path and needs a bound and an
            expiry it cannot be talked out of; the same choice pos-catalog and pos-customer made.
        -->
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
        </dependency>
        <!-- H2 for local dev (application-dev.yml) and tests; not configured in alpha/prod profiles -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!-- Spring Web MVC (admin/read controllers arrive in later CAP-317 slices) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <!-- Bean Validation for request DTOs -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <!-- Spring Data JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-flyway</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>
        <!-- Eureka Client -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- Resilience for vendor-facing outbound calls (CAP-317 slice 3: retry pre-send-only,
             circuit breaker per (vendorProfileId, capability)). GAVs mirror pos-document-helper
             and pos-tax exactly, deliberately unversioned.

             The version split noted here previously is resolved (#1260): the root POM now
             imports resilience4j-bom ahead of spring-cloud-dependencies, so the whole family
             resolves coherently at one version. Keep these unversioned. This module still
             configures its circuit breakers programmatically and leaves
             registerHealthIndicator off. -->
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-spring-boot4</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-retry</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-circuitbreaker</artifactId>
        </dependency>
        <!-- Prometheus scrape endpoint: /actuator/prometheus (ADR observability) -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc-openapi.version}</version>
        </dependency>
        <!-- Kotlin Reflect - required by springdoc-openapi 2.8+ -->
        <dependency>
            <groupId>org.jetbrains.kotlin</groupId>
            <artifactId>kotlin-reflect</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!-- Spring Boot Logging (includes SLF4J + Logback) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-logging</artifactId>
        </dependency>
        <!-- Shared DTOs (ApiError, UUIDv7Id for entities arriving in slice 2) -->
        <dependency>
            <groupId>com.positivity</groupId>
            <artifactId>pos-shared-dtos</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- Cross-module event contracts: supplier.pricecatalog.* payloads (ADR-0044 §3, CAP-318) -->
        <dependency>
            <groupId>com.positivity</groupId>
            <artifactId>pos-domain-events</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- Kafka producer for the transactional outbox drain (ADR-0044 §4) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-kafka</artifactId>
        </dependency>
        <!-- JSpecify for null safety annotations -->
        <dependency>
            <groupId>org.jspecify</groupId>
            <artifactId>jspecify</artifactId>
            <version>1.0.0</version>
        </dependency>
        <!-- JAXB for the EDIWheel C1 XML norms (CAP-320 #1226): order creation C1.0 and order
             status C1.1 are XML documents, unlike the B-series JSON reports. Versions come from
             the Spring Boot BOM, same as pos-documents — keep these unversioned. -->
        <dependency>
            <groupId>jakarta.xml.bind</groupId>
            <artifactId>jakarta.xml.bind-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.glassfish.jaxb</groupId>
            <artifactId>jaxb-runtime</artifactId>
        </dependency>
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- JUnit 5 for unit tests -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Spring Boot test slice support -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- @DataJpaTest slice (Boot 4 modular test starter) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- @WebMvcTest slice (Boot 4 modular test starter) for the admin controller tests -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <!--
              Activates the `test` Spring profile for every test in this module.

              Load-bearing, not convenience: AuditPayloadCipher requires an operator-provisioned encryption
              key unless `dev` or `test` is explicitly active (ADR-0050 §7, fail-closed), so without this
              every @DataJpaTest slice carrying ExchangeAuditEntity would fail to build its entity manager.

              Done here rather than with a src/test/resources/application.yml, which was the first attempt and
              was worse than the problem: a test-classpath application.yml SHADOWS the main one entirely, so
              the module's real configuration stops being loaded by any test. A duplicate-key error in the
              main application.yml then survived a fully green 519-test run and only surfaced when the app was
              actually started to generate the OpenAPI spec.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <systemPropertyVariables>
                        <spring.profiles.active>test</spring.profiles.active>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>repackage</id>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-maven-plugin</artifactId>
                <version>${springdoc-openapi-maven-plugin.version}</version>
            </plugin>
        </plugins>
    </build>
    <profiles>
        <profile>
            <id>openapi</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.springframework.boot</groupId>
                        <artifactId>spring-boot-maven-plugin</artifactId>
                        <configuration>
                            <jmxPort>9122</jmxPort>
                        </configuration>
                        <executions>
                            <execution>
                                <id>start-for-openapi</id>
                                <phase>pre-integration-test</phase>
                                <goals>
                                    <goal>start</goal>
                                </goals>
                                <configuration>
                                    <arguments>
                                        <argument>--server.port=8099</argument>
                                        <argument>--spring.profiles.active=dev</argument>
                                        <argument>--spring.jpa.hibernate.ddl-auto=update</argument>
                                        <argument>--spring.datasource.url=jdbc:h2:mem:openapi;MODE=PostgreSQL;DB_CLOSE_DELAY=-1</argument>
                                        <argument>--spring.datasource.driver-class-name=org.h2.Driver</argument>
                                        <argument>--spring.jpa.database-platform=org.hibernate.dialect.H2Dialect</argument>
                                        <argument>--spring.flyway.enabled=false</argument>
                                        <argument>--eureka.client.enabled=false</argument>
                                        <argument>--spring.boot.admin.client.enabled=false</argument>
                                        <argument>--logging.level.root=WARN</argument>
                                        <argument>--logging.level.org.hibernate.SQL=ERROR</argument>
                                    </arguments>
                                    <wait>8000</wait>
                                    <maxAttempts>180</maxAttempts>
                                </configuration>
                            </execution>
                            <execution>
                                <id>stop-after-openapi</id>
                                <phase>post-integration-test</phase>
                                <goals>
                                    <goal>stop</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.springdoc</groupId>
                        <artifactId>springdoc-openapi-maven-plugin</artifactId>
                        <version>${springdoc-openapi-maven-plugin.version}</version>
                        <configuration>
                            <apiDocsUrl>http://localhost:8099/v3/api-docs.yaml</apiDocsUrl>
                            <outputFileName>openapi.yaml</outputFileName>
                            <outputDir>${project.basedir}</outputDir>
                        </configuration>
                        <executions>
                            <execution>
                                <phase>integration-test</phase>
                                <goals>
                                    <goal>generate</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>sanitize-openapi</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target>
                                        <exec executable="python3" failonerror="false">
                                            <arg value="${maven.multiModuleProjectDirectory}/scripts/sanitize-openapi.py"/>
                                            <arg value="${project.basedir}/openapi.yaml"/>
                                        </exec>
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
