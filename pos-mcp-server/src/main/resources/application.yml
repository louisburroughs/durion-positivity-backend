spring:
  application:
    name: mcp-server
  jackson:
    default-property-inclusion: non_null
  flyway:
    locations: classpath:db/h2-migration
mcp:
  server:
    base-url: http://localhost:${server.port}
    message-endpoint: /mcp/message
    sse-endpoint: /mcp/sse
    open-api-path: /v3/api-docs
    discovery-timeout: 5s
    included-services:
      - event-receiver
    included-path-prefixes:
      - /v1/events/summary
  security:
    permit-all-transport: false
  rag:
    table-name: mcp_document_embedding
    dimension: 768
    index-type: ivfflat
    preload:
      docs:
        - id: "accounting.de-bookkeeping"
          source-path: "classpath:rag/de-bookkeeping-rag.md"
        - id: "inventory.inv-cntrl"
          source-path: "classpath:rag/inv-cntrl-rag.md"
    chunking:
      enabled: ${MCP_RAG_CHUNKING_ENABLED:true}
      max-segment-size: ${MCP_RAG_MAX_SEGMENT_SIZE:2000}
      max-overlap-size: ${MCP_RAG_MAX_OVERLAP_SIZE:200}
  agent:
    cache-ttl-minutes: 30
    max-cached-agents: 500
    memory-max-messages: 50
  tuning:
    enabled: true
    cron: "0 0 2 * * ?"
  model:
    fallback:
      enabled: ${MCP_MODEL_FALLBACK_ENABLED:false}
      secondary-model-name: ${OLLAMA_FALLBACK_MODEL:gpt-oss:120b}
      timeout: ${OLLAMA_FALLBACK_TIMEOUT:${OLLAMA_CHAT_TIMEOUT:180s}}

langchain4j:
  ollama:
    chat-model:
      base-url: ${OLLAMA_CHAT_BASE_URL:${OLLAMA_BASE_URL:http://localhost:11434}}
      model-name: ${OLLAMA_CHAT_MODEL:qwen3.5:cloud}
      temperature: 0.2
      timeout: ${OLLAMA_CHAT_TIMEOUT:180s}
    embedding-model:
      base-url: ${OLLAMA_EMBEDDING_BASE_URL:${OLLAMA_BASE_URL:http://localhost:11434}}
      model-name: ${OLLAMA_EMBEDDING_MODEL:nomic-embed-text}
      timeout: ${OLLAMA_EMBEDDING_TIMEOUT:30s}
    streaming-chat-model:
      base-url: ${OLLAMA_CHAT_BASE_URL:${OLLAMA_BASE_URL:http://localhost:11434}}
      model-name: ${OLLAMA_CHAT_MODEL:qwen3.5:cloud}
      temperature: 0.2
      timeout: ${OLLAMA_STREAMING_CHAT_TIMEOUT:${OLLAMA_CHAT_TIMEOUT:180s}}

exa:
  api-key: ${EXA_API_KEY:}
  base-url: https://api.exa.ai
  search-type: auto
  max-results: 5

server:
  port: 8086
  servlet:
    context-path: /

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:}
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true
    health-check-url-path: /actuator/health

management:
  server:
    port: 0
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when-authorized
  prometheus:
    metrics:
      export:
        enabled: true
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
    tagsSorter: alpha
  api-docs:
    path: /v3/api-docs

  default-produces-media-type: application/json
pos:
  accounting:
    base-url: ${POS_ACCOUNTING_BASE_URL:http://pos-accounting/v1/accounting}
    account-balance-uri-template: ${POS_ACCOUNTING_ACCOUNT_BALANCE_URI_TEMPLATE:/accounts/{accountId}/balance}
    journal-entries-search-uri-template: ${POS_ACCOUNTING_JOURNAL_ENTRIES_SEARCH_URI_TEMPLATE:/journal-entries/search?q={query}}
    financial-summary-uri-template: ${POS_ACCOUNTING_FINANCIAL_SUMMARY_URI_TEMPLATE:/summary/{period}}
  catalog:
    base-url: ${POS_CATALOG_BASE_URL:http://pos-catalog/v1/catalog}
    product-uri-template: ${POS_CATALOG_PRODUCT_URI_TEMPLATE:/products/{productId}}
    search-uri-template: ${POS_CATALOG_SEARCH_URI_TEMPLATE:/search?q={query}}
    category-uri-template: ${POS_CATALOG_CATEGORY_URI_TEMPLATE:/categories/{category}}
  customer:
    base-url: ${POS_CUSTOMER_BASE_URL:http://pos-customer/v1/customers}
    customer-uri-template: ${POS_CUSTOMER_URI_TEMPLATE:/{customerId}}
    search-uri-template: ${POS_CUSTOMER_SEARCH_URI_TEMPLATE:/search?q={query}}
    history-uri-template: ${POS_CUSTOMER_HISTORY_URI_TEMPLATE:/{customerId}/history}
  event-receiver:
    base-url: ${POS_EVENT_RECEIVER_BASE_URL:http://pos-event-receiver/v1/events}
    event-types-uri-template: ${POS_EVENT_RECEIVER_EVENT_TYPES_URI_TEMPLATE:/eventTypes}
    search-uri-template: ${POS_EVENT_RECEIVER_SEARCH_URI_TEMPLATE:/events/summary?q={query}}
    history-uri-template: ${POS_EVENT_RECEIVER_HISTORY_URI_TEMPLATE:/events/summary?entityId={entityId}}
  inventory:
    base-url: ${POS_INVENTORY_BASE_URL:http://pos-inventory/v1/inventory}
    stock-uri-template: ${POS_INVENTORY_STOCK_URI_TEMPLATE:/stock/{sku}}
    search-uri-template: ${POS_INVENTORY_SEARCH_URI_TEMPLATE:/search?q={query}}
    location-stock-uri-template: ${POS_INVENTORY_LOCATION_STOCK_URI_TEMPLATE:/locations/{locationId}/stock}
  invoice:
    base-url: ${POS_INVOICE_BASE_URL:http://pos-invoice/v1/invoices}
    invoice-uri-template: ${POS_INVOICE_URI_TEMPLATE:/{invoiceId}}
    search-uri-template: ${POS_INVOICE_SEARCH_URI_TEMPLATE:/search?q={query}}
    customer-invoices-uri-template: ${POS_INVOICE_CUSTOMER_INVOICES_URI_TEMPLATE:/customer/{customerId}}
  nlti:
    session:
      ttl-hours: 24
    rate-limit:
      per-session: 100
      max-tracked-sessions: 100000
  location:
    base-url: ${POS_LOCATION_BASE_URL:http://pos-location/v1/locations}
    location-uri-template: ${POS_LOCATION_URI_TEMPLATE:/{locationId}}
    search-uri-template: ${POS_LOCATION_SEARCH_URI_TEMPLATE:/search?q={query}}
    inventory-uri-template: ${POS_LOCATION_INVENTORY_URI_TEMPLATE:/{locationId}/inventory}
  order:
    base-url: ${POS_ORDER_BASE_URL:http://pos-order/v1/orders}
    order-uri-template: ${POS_ORDER_URI_TEMPLATE:/{orderId}}
    search-uri-template: ${POS_ORDER_SEARCH_URI_TEMPLATE:/search?q={query}}
  pricing:
    base-url: ${POS_PRICING_BASE_URL:http://pos-price/v1/pricing}
    sku-price-uri-template: ${POS_PRICING_SKU_PRICE_URI_TEMPLATE:/sku/{sku}}
    search-uri-template: ${POS_PRICING_SEARCH_URI_TEMPLATE:/search?q={query}}
    price-list-uri-template: ${POS_PRICING_PRICE_LIST_URI_TEMPLATE:/lists/{priceListId}}
  reporting:
    base-url: ${POS_REPORTING_BASE_URL:http://pos-accounting/v1/reporting}
    sales-report-uri-template: ${POS_REPORTING_SALES_REPORT_URI_TEMPLATE:/sales/{period}}
    inventory-report-uri-template: ${POS_REPORTING_INVENTORY_REPORT_URI_TEMPLATE:/inventory/{locationId}}
    revenue-report-uri-template: ${POS_REPORTING_REVENUE_REPORT_URI_TEMPLATE:/revenue/{period}}
  security:
    users:
      base-url: ${POS_SECURITY_USERS_BASE_URL:http://localhost:8086/v1/users}
    audit:
      base-url: ${POS_SECURITY_AUDIT_BASE_URL:http://localhost:8086/v1/audit}
    user-permissions-path-template: ${POS_SECURITY_USER_PERMISSIONS_PATH_TEMPLATE:/{userId}/permissions}
    audit-search-uri-template: ${POS_SECURITY_AUDIT_SEARCH_URI_TEMPLATE:/events?eventType={query}}
  hr:
    employee-base-url: ${POS_HR_EMPLOYEE_BASE_URL:http://localhost:8085/v1/people/employees}
    people-base-url: ${POS_HR_PEOPLE_BASE_URL:http://localhost:8085/v1/people}
    availability-base-url: ${POS_HR_AVAILABILITY_BASE_URL:http://localhost:8085/v1/people}
    employee-uri-template: ${POS_HR_EMPLOYEE_URI_TEMPLATE:/{employeeId}}
    search-uri-template: ${POS_HR_SEARCH_URI_TEMPLATE:?q={query}}
    schedule-uri-template: ${POS_HR_SCHEDULE_URI_TEMPLATE:/availability?employeeId={employeeId}}
  shopmanager:
    base-url: ${POS_SHOPMANAGER_BASE_URL:http://pos-shop-manager/v1/shop}
    status-uri-template: ${POS_SHOPMANAGER_STATUS_URI_TEMPLATE:/{shopId}/status}
    queue-uri-template: ${POS_SHOPMANAGER_QUEUE_URI_TEMPLATE:/{shopId}/queue}
    search-uri-template: ${POS_SHOPMANAGER_SEARCH_URI_TEMPLATE:/search?q={query}}
  tax:
    base-url: ${POS_TAX_BASE_URL:http://pos-tax/v1/tax}
    tax-rate-uri-template: ${POS_TAX_RATE_URI_TEMPLATE:/rates/{locationId}}
    tax-calculate-uri-template: ${POS_TAX_CALCULATE_URI_TEMPLATE:/calculate?amount={amount}&locationId={locationId}}
    tax-summary-uri-template: ${POS_TAX_SUMMARY_URI_TEMPLATE:/summary/{period}}
  vehicle:
    base-url: ${POS_VEHICLE_BASE_URL:http://pos-customer/v1/vehicles}
    vehicle-uri-template: ${POS_VEHICLE_URI_TEMPLATE:/{vehicleId}}
    search-uri-template: ${POS_VEHICLE_SEARCH_URI_TEMPLATE:/search?q={query}}
    customer-vehicles-uri-template: ${POS_VEHICLE_CUSTOMER_VEHICLES_URI_TEMPLATE:/customer/{customerId}}
  workorder:
    base-url: ${POS_WORKORDER_BASE_URL:http://pos-workorder/v1/workorders}
    workorder-uri-template: ${POS_WORKORDER_URI_TEMPLATE:/{workorderId}}
    search-uri-template: ${POS_WORKORDER_SEARCH_URI_TEMPLATE:/search?q={query}}
    status-uri-template: ${POS_WORKORDER_STATUS_URI_TEMPLATE:/{workorderId}/status}
  show-actuator: true

logging:
  level:
    com:
      positivity:
        mcp: DEBUG
    org:
      springframework:
        web: INFO
        cloud: INFO
