# issue-questions.md (Responses completed)

| Issue Number | Origin Story Number | Domain(s) | Question Number | Question | Responses | Follow-up Questions | Status |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **324** | #178 | inventory | 1 | What are the specific, nuanced requirements, business rules, or edge cases from original story #236? | Treat as *inventory consumption & costing* with: partial picks allowed; substitutions allowed with traceability; negative inventory allowed only if explicitly enabled per location/item class; serial/lot enforcement where required; returns/restocks must reverse cost layers; backdated transactions require admin + audit + period checks. | Which item classes require lot/serial? Is negative inventory allowed for any locations initially? | ✅ COMPLETED |
|  |  |  | 2 | What is the authoritative source for an item's cost (COGS) at the time of consumption? | Inventory is source-of-truth for *cost layers*; cost at consumption is computed from the valuation method (default **Weighted Average** or **FIFO**, configurable per org/location) and persisted on the consumption/ledger entry. Accounting consumes the posted cost, does not recompute. | Choose default valuation method for v1 (FIFO vs Weighted Average). Any per-item overrides? | ✅ COMPLETED |
|  |  |  | 3 | Should the entire consumption transaction be atomic? | Yes for internal consistency: (1) validate availability/policies, (2) write immutable ledger entries + consumption record, (3) update on-hand, (4) enqueue domain event via **outbox** in same DB transaction. External calls must be async (no distributed transactions). | Any cases where you want "best effort" consumption even if ledger write fails? (Strongly discouraged.) | ✅ COMPLETED |
| **321** | #127 | accounting | 1 | What specific user roles will be granted MANAGING and REOPEN permissions? | Default mapping: **ADMIN** (all), **ACCOUNTING_MANAGER** (manage + reopen), **MANAGER** (reopen workorders within location + within time window), **ACCOUNTING_CLERK** (no reopen; can post adjustments only with approval). Permissions are explicit RBAC claims; do not infer from role names. | Do you want MANAGER reopen limited to same-day/24h? Any "SERVICE_ADVISOR" role? | ✅ COMPLETED |
|  |  |  | 2 | What entity represents a Business Unit (Location, legal entity, etc.)? | Use **Location** as the operational Business Unit for POS/workexec. Separately model **LegalEntity/Company** for accounting reporting/tax identity. A WorkOrder references Location; accounting postings include both LocationId and LegalEntityId (derived from Location). | Do you need multi-legal-entity support in v1, or single legal entity? | ✅ COMPLETED |
|  |  |  | 3 | Is the proposed error response sufficient for all upstream clients? | Standardize on **RFC 7807** (`application/problem+json`) with stable `code`, `message`, `details`, `correlationId`, and `retryable` boolean. Include field-level validation errors when applicable. | Which clients require backward compatibility with any existing error envelope? | ✅ COMPLETED |
|  |  |  | 4 | Can accounting periods be created for future dates or back-dated? | Yes: allow creating future periods. Allow back-dated creation only for ADMIN/ACCOUNTING_MANAGER, with audit logging, and never if it would overlap an already-closed official period without an override workflow. | Do you need fiscal calendar templates (4-4-5, etc.)? | ✅ COMPLETED |
|  |  |  | 5 | Confirm that domain:accounting is the correct primary domain. | **domain:accounting is correct** |  | ✅ COMPLETED |
| **319** | #173 | workexec | 1 | What is the specific mechanism for granting PRICE_OVERRIDE permission? | RBAC permission `pricing:override` granted via role assignment + optional policy guardrails: thresholds require approval (`pricing:override:approve`) and capture `overrideReasonCode`, `overrideJustification`, `approvedBy`, timestamps. | What thresholds apply (%, amount) and are they per location or global? | ✅ COMPLETED |
|  |  |  | 2 | What is the definitive list of valid overrideReasonCode values? | Start with enums: `MATCH_COMPETITOR`, `CUSTOMER_RETENTION`, `WARRANTY`, `PROMOTION_EXCEPTION`, `DAMAGE/DEFECT`, `PRICE_BOOK_ERROR`, `MANUAL_CORRECTION`, `MANAGER_DISCRETION`, `GOODWILL`. Treat list as configurable (admin-managed) but with stable codes. | Who owns the managed list (pricing vs workexec), and do you need per-location enablement? | ✅ COMPLETED |
|  |  |  | 3 | Is the ALLOW_NON_CATALOG_PARTS policy system-wide or per location? | Per-location policy with optional org default. Enforced in workexec at line-add time; also logged for audit. | Are non-catalog parts allowed only for specific roles? | ✅ COMPLETED |
|  |  |  | 4 | What is the source of truth for the taxCode field? | Source-of-truth is **Product + Tax configuration**: Product provides default `taxCategory/taxCode`; Tax engine resolves final taxability based on jurisdiction (location/service address) and customer exemptions. Workexec stores the resolved code used for calculation on the snapshot. | Are you using an external tax provider in v1? If yes, which one? | ✅ COMPLETED |

## Summary

All clarification issues have been processed and applied to their origin stories:

- **Issue #324** → Origin #178: Inventory consumption model with cost layers, valuation methods, and atomicity rules ✅
- **Issue #321** → Origin #127: Accounting period management with RBAC, dual-entity model, RFC 7807 errors ✅
- **Issue #319** → Origin #173: Price override mechanism, reason codes, non-catalog policies, tax resolution ✅

All origin stories updated to `status:needs-review` pending follow-up clarifications.
All clarification issues closed as completed.
