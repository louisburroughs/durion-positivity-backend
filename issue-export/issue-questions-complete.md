# issue-questions.md (Responses completed)

| Issue Number | Origin Story Number | Domain(s) | Question Number | Question | Responses | Follow-up Questions |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **324** | #178 | inventory | 1 | What are the specific, nuanced requirements, business rules, or edge cases from original story #236? | Prefer append-only inventory ledger movements (receipt/transfer/adjust/consume/return) with effective-dated rules; enforce lot/serial where required; emit events via outbox. | Do you need lot/serial tracking and FEFO rules in v1? |
|  |  |  | 2 | What is the authoritative source for an item's cost (COGS) at the time of consumption? | Inventory is source-of-truth for valuation (FIFO/Weighted Avg); compute and persist the cost-at-consumption on immutable consumption/ledger entries; accounting consumes posted costs, does not recalc. | Default valuation method for v1 (FIFO vs Weighted Avg), and any per-location/per-item overrides? |
|  |  |  | 3 | Should the entire consumption transaction be atomic? | Use a single local DB transaction for state + ledger writes and an outbox record; publish events asynchronously (no distributed transactions). Ensure idempotency keys for retries. | Any flows that must proceed on partial failure (generally discouraged)? |
| **321** | #127 | accounting | 1 | What specific user roles will be granted MANAGING and REOPEN permissions? | Use explicit RBAC permissions (string scopes) decoupled from role names; support location/legal-entity scoping and effective-dated grants; audit all privileged actions. | Which roles exist at launch (e.g., SERVICE_ADVISOR, INVENTORY_MANAGER), and which are global vs location-scoped? |
|  |  |  | 2 | What entity represents a Business Unit (Location, legal entity, etc.)? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
|  |  |  | 3 | Is the proposed error response sufficient for all upstream clients? | Use RFC 7807 problem+json with stable machine codes, correlationId, and retryable flag; include field-level validation errors; never leak internals. | Any clients that require a legacy error envelope? |
|  |  |  | 4 | Can accounting periods be created for future dates or back-dated? | Model periods with start/end dates and OPEN/CLOSED status; block posting into CLOSED periods by default; allow override only with explicit permission, reason, and audit trail. | Preferred adjustment policy: reopen period vs post adjusting entry in next open period? |
|  |  |  | 5 | Confirm that domain:accounting is the correct primary domain. | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **319** | #173 | workexec | 1 | What is the specific mechanism for granting PRICE_OVERRIDE permission? | Use explicit RBAC permissions (string scopes) decoupled from role names; support location/legal-entity scoping and effective-dated grants; audit all privileged actions. | Which roles exist at launch (e.g., SERVICE_ADVISOR, INVENTORY_MANAGER), and which are global vs location-scoped? |
|  |  |  | 2 | What is the definitive list of valid overrideReasonCode values? | Use managed, stable reasonCode enums (configurable list with immutable codes) and require justification notes for privileged actions; audit who approved and when. | Which reason codes are mandatory for launch, and who owns maintaining the list? |
|  |  |  | 3 | Is the ALLOW_NON_CATALOG_PARTS policy system-wide or per location? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
|  |  |  | 4 | What is the source of truth for the taxCode field? | Taxability and rates are resolved by the Tax engine using jurisdiction + customer exemptions; store the resolved tax codes/rates on the pricing/invoice snapshot (do not recompute later). | Internal tax rules vs external provider (Avalara/Vertex/etc.) for v1? |
| **318** | #171 | pricing | 1 | What is the business policy for handling a taxable item missing a tax code? | Prefer deterministic rule evaluation with explicit precedence (SKU>category>global); persist applied rule traces and snapshots; block finalization if required base data is missing unless override is permitted. | Are promotions stackable by default, and what are override thresholds? | ✅ COMPLETED
|  |  |  | 2 | What is the authoritative rounding policy for final totals? | Use currency-scale rounding (e.g., USD=2) with HALF_UP; round tax per line, then sum; persist explicit rounding deltas for audit/replay. | Any jurisdictions requiring HALF_EVEN (banker’s rounding) or cash-rounding increments (e.g., 0.05)? |
|  |  |  | 3 | Does the system need to support tax-inclusive pricing models now? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **317** | #168 | workexec | 1 | What specific fields and conditions define a "complete" estimate for submission? | Prefer immutable snapshots at approval/invoice boundaries; changes after approval use revision/change-request workflows with explicit permissions and audit; integrate via events with idempotency. | Which actions are allowed after customer approval (reprice, substitute, add/remove lines)? | ✅ COMPLETED
| **316** | #161 | workexec | 1 | What is the source for technician availability and the precise business rule for blocking? | Prefer immutable snapshots at approval/invoice boundaries; changes after approval use revision/change-request workflows with explicit permissions and audit; integrate via events with idempotency. | Which actions are allowed after customer approval (reprice, substitute, add/remove lines)? | ✅ COMPLETED
|  |  |  | 2 | What are the required notification channels, who configures them, and what is the content? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **315** | #140 | accounting | 1 | What are the precise conditions (balances, usage, templates) for account deactivation? | Prefer double-entry journal posting from immutable source events; enforce debits=credits, period controls, and traceability to source documents; never mutate posted journals—use reversals/adjustments. | Single legal entity vs multi-entity at launch? | ✅ COMPLETED
| **314** | #137 | accounting | 1 | What is the explicit policy for handling mapping failures (Suspense vs. Rejection)? | Default to reject with actionable error; optionally post to a configured Suspense account only for approved eventTypes, and always emit an exception item for resolution. | Which eventTypes are allowed to post to suspense vs must hard-fail? | ✅ COMPLETED
| **313** | #134 | accounting | 1 | Which specific dimensions must be supported for filtering (Location, Project, etc.)? | Prefer double-entry journal posting from immutable source events; enforce debits=credits, period controls, and traceability to source documents; never mutate posted journals—use reversals/adjustments. | Single legal entity vs multi-entity at launch? | ✅ COMPLETED
|  |  |  | 2 | What is the precise model for access control granularity? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
|  |  |  | 3 | What is the exact column layout and naming for the CSV export? | Use RFC4180 UTF-8 CSV with stable headers, explicit currency/amount columns, and cursor-based paging for large exports; protect via permissions and audit exports. | Do you need separate export formats (e.g., IIF/OFX) or only CSV initially? |
|  |  |  | 4 | How is a "period" officially defined (Calendar vs. Fiscal)? | Model periods with start/end dates and OPEN/CLOSED status; block posting into CLOSED periods by default; allow override only with explicit permission, reason, and audit trail. | Preferred adjustment policy: reopen period vs post adjusting entry in next open period? |
|  |  |  | 5 | What is the contract for the "Source Event" payload for drilldowns? | Standardize on an idempotent event envelope: eventId(UUIDv7), eventType(enum), schemaVersion, occurredAt(UTC), producedBy, correlationId, idempotencyKey, payload(JSON). Version payload per eventType. | Target broker/transport (Kafka vs SNS/SQS/Rabbit) and required retention for replay? |
| **312** | #120 | accounting | 1 | Under what conditions is negative inventory permissible and who defines this? | Prefer double-entry journal posting from immutable source events; enforce debits=credits, period controls, and traceability to source documents; never mutate posted journals—use reversals/adjustments. | Single legal entity vs multi-entity at launch? | ✅ COMPLETED
| **311** | #114 | accounting | 1 | Confirm if Accounting is the primary domain for PaymentApplication state. | Prefer double-entry journal posting from immutable source events; enforce debits=credits, period controls, and traceability to source documents; never mutate posted journals—use reversals/adjustments. | Single legal entity vs multi-entity at launch? | ✅ COMPLETED
|  |  |  | 2 | What is the formal event or API contract between Payment and Accounting? | Standardize on an idempotent event envelope: eventId(UUIDv7), eventType(enum), schemaVersion, occurredAt(UTC), producedBy, correlationId, idempotencyKey, payload(JSON). Version payload per eventType. | Target broker/transport (Kafka vs SNS/SQS/Rabbit) and required retention for replay? |
|  |  |  | 3 | How are overpayments handled (CustomerCredit entity vs. left on payment)? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
|  |  |  | 4 | Define the business process, authorization, and audit trail for reversals. | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **310** | #109 | crm | 1 | How are conflicting primitive attributes handled during a merge? | Use deterministic survivor-based merge with field-level precedence rules and full audit; mark sources as MERGED (read-only) and redirect lookups; never hard-delete. | Do any sources have higher precedence (e.g., DMS import vs manual entry)? | ✅ COMPLETED
|  |  |  | 2 | What is the precise final state of a source record after a merge? | Use deterministic survivor-based merge with field-level precedence rules and full audit; mark sources as MERGED (read-only) and redirect lookups; never hard-delete. | Do any sources have higher precedence (e.g., DMS import vs manual entry)? | ✅ COMPLETED |
|  |  |  | 3 | Is there a requirement for administrative "un-merge" capabilities? | Use deterministic survivor-based merge with field-level precedence rules and full audit; mark sources as MERGED (read-only) and redirect lookups; never hard-delete. | Do any sources have higher precedence (e.g., DMS import vs manual entry)? | ✅ COMPLETED |
| **309** | #106 | crm | 1 | Confirm separation of "kind" and "label" for contacts and if labels are configurable. | Prefer survivorship/identity resolution with immutable external identifiers; avoid hard deletes; enforce normalization (E.164 phones, email); provide merge workflow with audit. | Is email unique globally, per legal entity, or per account? | ✅ COMPLETED
|  |  |  | 2 | Confirm the "single primary per kind" demotion rule. | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **308** | #97 | pricing | 1 | How is customer eligibility for promotions defined? | Prefer deterministic rule evaluation with explicit precedence (SKU>category>global); persist applied rule traces and snapshots; block finalization if required base data is missing unless override is permitted. | Are promotions stackable by default, and what are override thresholds? | ✅ COMPLETED
|  |  |  | 2 | Can a single promotion contain multiple discount types? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | Is storeCode mandatory, and does null imply all locations? | Allow null/empty only when a dedicated ‘anonymous/cash’ mode is explicitly flagged; otherwise enforce required identifiers; validate at API boundary with clear errors. | Do you prefer a real ‘Cash Customer’ record instead of null identifiers? | ✅ COMPLETED |
|  |  |  | 4 | Is usageLimit tracked globally or per-customer? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **307** | #172 | workexec | 1 | What is the hierarchy and fallback logic for determine default labor rates? | Prefer immutable snapshots at approval/invoice boundaries; changes after approval use revision/change-request workflows with explicit permissions and audit; integrate via events with idempotency. | Which actions are allowed after customer approval (reprice, substitute, add/remove lines)? | ✅ COMPLETED
|  |  |  | 2 | Are units and rates editable for flat-rate labor items? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **306** | #170 | workexec | 1 | What is the policy for revisions on already approved/converted estimates? | Prefer immutable snapshots at approval/invoice boundaries; changes after approval use revision/change-request workflows with explicit permissions and audit; integrate via events with idempotency. | Which actions are allowed after customer approval (reprice, substitute, add/remove lines)? | ✅ COMPLETED
| **305** | #169 | workexec | 1 | Should summary generation fail or use defaults if legal terms are missing? | Prefer immutable snapshots at approval/invoice boundaries; changes after approval use revision/change-request workflows with explicit permissions and audit; integrate via events with idempotency. | Which actions are allowed after customer approval (reprice, substitute, add/remove lines)? | ✅ COMPLETED |
| **304** | #160 | workexec | 1 | Is "auto-start on first labor entry" in scope for this story? | Prefer immutable snapshots at approval/invoice boundaries; changes after approval use revision/change-request workflows with explicit permissions and audit; integrate via events with idempotency. | Which actions are allowed after customer approval (reprice, substitute, add/remove lines)? | ✅ COMPLETED |
|  |  |  | 2 | What is the exhaustive list of statuses from which a work order can be started? | Implement a finite state machine with enumerated states + allowed transitions; store transition audit (who/when/why); keep enums stable and extend via config only where safe. | Any must-have states/transitions for v1 beyond the proposed set? | ✅ COMPLETED |
|  |  |  | 3 | What is the complete list of valid in-progress sub-statuses? | Implement a finite state machine with enumerated states + allowed transitions; store transition audit (who/when/why); keep enums stable and extend via config only where safe. | Any must-have states/transitions for v1 beyond the proposed set? |
|  |  |  | 4 | How does the system technically identify a "pending approval change request"? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **303** | #158 | workexec | 1 | Should "Issued" and "Consumed" be distinct events or atomic? | Use a single local DB transaction for state + ledger writes and an outbox record; publish events asynchronously (no distributed transactions). Ensure idempotency keys for retries. | Any flows that must proceed on partial failure (generally discouraged)? | ✅ COMPLETED |
|  |  |  | 2 | What is the precise deterministic key format for part usage? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
|  |  |  | 3 | Where is the rule for WIP vs. COGS accounting impact configured? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **302** | #155 | workexec | 1 | What is the authoritative source for VisibilityPolicy data? | Security/Policy service is authoritative for permissions and visibility rules; domain services enforce server-side and cache with short TTL + invalidation events. | Desired cache TTL and invalidation mechanism? | ✅ COMPLETED |
|  |  |  | 2 | Is the policy configurable per-field or per-role flag? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
|  |  |  | 3 | Should implementation use a single endpoint or role-specific endpoints? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **301** | #152 | workexec | 1 | What is the policy for handling Completed but Unauthorized items? | Prefer immutable snapshots at approval/invoice boundaries; changes after approval use revision/change-request workflows with explicit permissions and audit; integrate via events with idempotency. | Which actions are allowed after customer approval (reprice, substitute, add/remove lines)? | ✅ COMPLETED |
|  |  |  | 2 | What is the required workflow when a price or tax variance is detected? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
|  |  |  | 3 | What roles/permissions are required for corrections on finalized orders? | Use explicit RBAC permissions (string scopes) decoupled from role names; support location/legal-entity scoping and effective-dated grants; audit all privileged actions. | Which roles exist at launch (e.g., SERVICE_ADVISOR, INVENTORY_MANAGER), and which are global vs location-scoped? |
| **300** | #150 | workexec | 1 | What is the exact target state for a reopened work order? | Prefer immutable snapshots at approval/invoice boundaries; changes after approval use revision/change-request workflows with explicit permissions and audit; integrate via events with idempotency. | Which actions are allowed after customer approval (reprice, substitute, add/remove lines)? | ✅ COMPLETED |
|  |  |  | 2 | Which specific fields become editable after reopening? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **299** | #148 | accounting | 1 | What is the authoritative rounding policy for financial calculations? | Use currency-scale rounding (e.g., USD=2) with HALF_UP; round tax per line, then sum; persist explicit rounding deltas for audit/replay. | Any jurisdictions requiring HALF_EVEN (banker’s rounding) or cash-rounding increments (e.g., 0.05)? | ✅ COMPLETED |
|  |  |  | 2 | Is the tax rule source internal or a third-party provider? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What is the complete, canonical list of varianceReasonCodes to detect? | Use managed, stable reasonCode enums (configurable list with immutable codes) and require justification notes for privileged actions; audit who approved and when. | Which reason codes are mandatory for launch, and who owns maintaining the list? | ✅ COMPLETED |
|  |  |  | 4 | What specific fields are mandatory for successful tax calculation? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **298** | #143 | accounting | 1 | What is the specific contract/schema for event payloads? | Standardize on an idempotent event envelope: eventId(UUIDv7), eventType(enum), schemaVersion, occurredAt(UTC), producedBy, correlationId, idempotencyKey, payload(JSON). Version payload per eventType. | Target broker/transport (Kafka vs SNS/SQS/Rabbit) and required retention for replay? | ✅ COMPLETED |
|  |  |  | 2 | Which specific message broker technology is targeted? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What is the idempotency strategy for duplicate event submissions? | Require idempotencyKey on all writes/events; store (key, payloadHash, resultRef) with retention; duplicates return the original result (or 409 with DUPLICATE_EVENT code) and are audited. | Prefer ‘idempotent success’ vs ‘409 conflict’ for duplicates? | ✅ COMPLETED |
|  |  |  | 4 | What security mechanism will secure the synchronous endpoint? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **297** | #142 | accounting | 1 | What is the required mechanism for "flagging" duplicate events? | Require idempotencyKey on all writes/events; store (key, payloadHash, resultRef) with retention; duplicates return the original result (or 409 with DUPLICATE_EVENT code) and are audited. | Prefer ‘idempotent success’ vs ‘409 conflict’ for duplicates? | ✅ COMPLETED |
|  |  |  | 2 | Should the entire payload or a subset be hashed for conflict detection? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What is the data retention policy for the idempotency store? | Require idempotencyKey on all writes/events; store (key, payloadHash, resultRef) with retention; duplicates return the original result (or 409 with DUPLICATE_EVENT code) and are audited. | Prefer ‘idempotent success’ vs ‘409 conflict’ for duplicates? | ✅ COMPLETED |
| **296** | #141 | accounting | 1 | What is the policy for handling unknown event types? | Prefer double-entry journal posting from immutable source events; enforce debits=credits, period controls, and traceability to source documents; never mutate posted journals—use reversals/adjustments. | Single legal entity vs multi-entity at launch? | ✅ COMPLETED |
|  |  |  | 2 | What are the exhaustive financial consistency checks and tolerances? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What is the canonical list of actionable error codes for failures? | Use RFC 7807 problem+json with stable machine codes, correlationId, and retryable flag; include field-level validation errors; never leak internals. | Any clients that require a legacy error envelope? | ✅ COMPLETED |
| **295** | #139 | accounting | 1 | What is the policy for handling date-range overlaps in mappings? | Prefer double-entry journal posting from immutable source events; enforce debits=credits, period controls, and traceability to source documents; never mutate posted journals—use reversals/adjustments. | Single legal entity vs multi-entity at launch? | ✅ COMPLETED |
|  |  |  | 2 | What is the system behavior for an unmappable transaction? | Use a single local DB transaction for state + ledger writes and an outbox record; publish events asynchronously (no distributed transactions). Ensure idempotency keys for retries. | Any flows that must proceed on partial failure (generally discouraged)? | ✅ COMPLETED |
|  |  |  | 3 | What is the definitive list of financial dimensions to capture? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **294** | #136 | accounting | 1 | Confirm the policy for closed period postings (Strict Block vs. Override). | Model periods with start/end dates and OPEN/CLOSED status; block posting into CLOSED periods by default; allow override only with explicit permission, reason, and audit trail. | Preferred adjustment policy: reopen period vs post adjusting entry in next open period? | ✅ COMPLETED |
| **293** | #131 | accounting | 1 | Is it acceptable to scope this story only to accounting functions? | Prefer double-entry journal posting from immutable source events; enforce debits=credits, period controls, and traceability to source documents; never mutate posted journals—use reversals/adjustments. | Single legal entity vs multi-entity at launch? | ✅ COMPLETED |
|  |  |  | 2 | What is the business trigger to initiate a cash refund? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | Provide specific period-close policies for adjustments. | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | Does a Credit Memo require an approval workflow? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **292** | #130 | accounting | 1 | Which is the primary trigger for bill creation (Goods vs. Invoice)? | Prefer double-entry journal posting from immutable source events; enforce debits=credits, period controls, and traceability to source documents; never mutate posted journals—use reversals/adjustments. | Single legal entity vs multi-entity at launch? | ✅ COMPLETED |
|  |  |  | 2 | What is the exact initial state of a newly created bill? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What is the process if event quantities/prices mismatch the PO? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **291** | #125 | accounting | 1 | What is the COA structure for mapping lines to statements? | Prefer double-entry journal posting from immutable source events; enforce debits=credits, period controls, and traceability to source documents; never mutate posted journals—use reversals/adjustments. | Single legal entity vs multi-entity at launch? | ✅ COMPLETED |
|  |  |  | 2 | Which accounting standard (GAAP/IFRS) should be followed? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What are the specific roles and permissions for report viewing? | Use explicit RBAC permissions (string scopes) decoupled from role names; support location/legal-entity scoping and effective-dated grants; audit all privileged actions. | Which roles exist at launch (e.g., SERVICE_ADVISOR, INVENTORY_MANAGER), and which are global vs location-scoped? | ✅ COMPLETED |
|  |  |  | 4 | Which specific export formats (PDF, CSV, etc.) are required? | Use RFC4180 UTF-8 CSV with stable headers, explicit currency/amount columns, and cursor-based paging for large exports; protect via permissions and audit exports. | Do you need separate export formats (e.g., IIF/OFX) or only CSV initially? |
|  |  |  | 5 | How should multi-entity/currency scenarios be handled? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **290** | #124 | accounting | 1 | What are the data types and sources for traceability IDs? | Prefer double-entry journal posting from immutable source events; enforce debits=credits, period controls, and traceability to source documents; never mutate posted journals—use reversals/adjustments. | Single legal entity vs multi-entity at launch? | ✅ COMPLETED |
|  |  |  | 2 | Does the "explainability view" require a public API or just data structure? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What system is the authoritative source for business event details? | Treat the producing domain as authoritative for business semantics; downstream domains store immutable snapshots needed for posting/audit and link back for drilldown. | Which domain should be the long-term owner for this data (if different from current)? | ✅ COMPLETED |
| **289** | #123 | accounting | 1 | Which file formats (CSV, OFX, etc.) must be supported for import? | Use RFC4180 UTF-8 CSV with stable headers, explicit currency/amount columns, and cursor-based paging for large exports; protect via permissions and audit exports. | Do you need separate export formats (e.g., IIF/OFX) or only CSV initially? | ✅ COMPLETED |
|  |  |  | 2 | Is there a requirement for a matching tolerance (e.g., $0.01)? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | Can default GL accounts be pre-configured for adjustment types? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | What is the contract for retrieving unreconciled transactions? | Use a single local DB transaction for state + ledger writes and an outbox record; publish events asynchronously (no distributed transactions). Ensure idempotency keys for retries. | Any flows that must proceed on partial failure (generally discouraged)? |
|  |  |  | 5 | What specific fields must be included in the Reconciliation Report? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **288** | #121 | accounting | 1 | Where is the inventory valuation method configured and maintained? | Prefer double-entry journal posting from immutable source events; enforce debits=credits, period controls, and traceability to source documents; never mutate posted journals—use reversals/adjustments. | Single legal entity vs multi-entity at launch? | ✅ COMPLETED |
|  |  |  | 2 | What determined whether to post to COGS vs. WIP? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | How are specific GL account identifiers for assets determined? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **287** | #119 | accounting | 1 | What specific fields are guaranteed in the finalCostSummary? | Prefer double-entry journal posting from immutable source events; enforce debits=credits, period controls, and traceability to source documents; never mutate posted journals—use reversals/adjustments. | Single legal entity vs multi-entity at launch? | ✅ COMPLETED |
|  |  |  | 2 | What is the logic to determine the destination GL account for WIP? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **286** | #118 | accounting | 1 | What are the specific GL IDs for standard WIP and COGS accounts? | Prefer double-entry journal posting from immutable source events; enforce debits=credits, period controls, and traceability to source documents; never mutate posted journals—use reversals/adjustments. | Single legal entity vs multi-entity at launch? | ✅ COMPLETED |
|  |  |  | 2 | Is event trust sufficient, or is an external authorization check required? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What is the behavior if the workorderId is not found? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **285** | #116 | accounting | 1 | What specific GL IDs should be used for AR, Revenue, and Tax? | Prefer double-entry journal posting from immutable source events; enforce debits=credits, period controls, and traceability to source documents; never mutate posted journals—use reversals/adjustments. | Single legal entity vs multi-entity at launch? | ✅ COMPLETED |
|  |  |  | 2 | What is the authoritative source for adjustment authorization? | Treat the producing domain as authoritative for business semantics; downstream domains store immutable snapshots needed for posting/audit and link back for drilldown. | Which domain should be the long-term owner for this data (if different from current)? | ✅ COMPLETED |
|  |  |  | 3 | Are pattern rules identical for Adjusted vs. CreditMemo events? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | What is the behavior for adjustments that increase the total amount? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **284** | #115 | accounting | 1 | What is the correct policy for currency mismatches? | Prefer double-entry journal posting from immutable source events; enforce debits=credits, period controls, and traceability to source documents; never mutate posted journals—use reversals/adjustments. | Single legal entity vs multi-entity at launch? | ✅ COMPLETED |
|  |  |  | 2 | Is a null customerId acceptable for payments? | Allow null/empty only when a dedicated ‘anonymous/cash’ mode is explicitly flagged; otherwise enforce required identifiers; validate at API boundary with clear errors. | Do you prefer a real ‘Cash Customer’ record instead of null identifiers? | ✅ COMPLETED |
| **283** | #113 | accounting | 1 | What are the reasonCodes and required accounting treatments? | Use managed, stable reasonCode enums (configurable list with immutable codes) and require justification notes for privileged actions; audit who approved and when. | Which reason codes are mandatory for launch, and who owns maintaining the list? | ✅ COMPLETED |
|  |  |  | 2 | What is the behavior for refunds on already credited invoices? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **282** | #112 | crm | 1 | Which domain is the owner for Commercial Account (Party) entities? | Prefer survivorship/identity resolution with immutable external identifiers; avoid hard deletes; enforce normalization (E.164 phones, email); provide merge workflow with audit. | Is email unique globally, per legal entity, or per account? | ✅ COMPLETED |
|  |  |  | 2 | What specific fields constitute "default billing terms"? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | Confirm the logic for duplicate name/contact detection. | Require idempotencyKey on all writes/events; store (key, payloadHash, resultRef) with retention; duplicates return the original result (or 409 with DUPLICATE_EVENT code) and are audited. | Prefer ‘idempotent success’ vs ‘409 conflict’ for duplicates? | ✅ COMPLETED |
|  |  |  | 4 | How should ExternalIdentifiers be managed and constrained? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **281** | #108 | crm | 1 | Which domain is authoritative for managing contact roles? | Use explicit RBAC permissions (string scopes) decoupled from role names; support location/legal-entity scoping and effective-dated grants; audit all privileged actions. | Which roles exist at launch (e.g., SERVICE_ADVISOR, INVENTORY_MANAGER), and which are global vs location-scoped? | ✅ COMPLETED |
|  |  |  | 2 | What is the definitive list of contact roles required at launch? | Use explicit RBAC permissions (string scopes) decoupled from role names; support location/legal-entity scoping and effective-dated grants; audit all privileged actions. | Which roles exist at launch (e.g., SERVICE_ADVISOR, INVENTORY_MANAGER), and which are global vs location-scoped? | ✅ COMPLETED |
|  |  |  | 3 | Confirm the automatic demotion logic for primary contacts. | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | Is the billing contact rule global or per-account? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **280** | #105 | crm | 1 | Is VIN uniqueness global or only within a single customer account? | Prefer survivorship/identity resolution with immutable external identifiers; avoid hard deletes; enforce normalization (E.164 phones, email); provide merge workflow with audit. | Is email unique globally, per legal entity, or per account? | ✅ COMPLETED |
| **279** | #103 | crm | 1 | What is the required ranking algorithm for search results? | Prefer survivorship/identity resolution with immutable external identifiers; avoid hard deletes; enforce normalization (E.164 phones, email); provide merge workflow with audit. | Is email unique globally, per legal entity, or per account? | ✅ COMPLETED |
|  |  |  | 2 | Define "partial" search (contains vs. starts with) and min length. | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What fields must be in the "full vehicle + owner snapshot"? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | What is the maximum number of results and is pagination needed? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **278** | #102 | crm | 1 | Is the preference list fixed or must it use a flexible JSONB model? | Prefer survivorship/identity resolution with immutable external identifiers; avoid hard deletes; enforce normalization (E.164 phones, email); provide merge workflow with audit. | Is email unique globally, per legal entity, or per account? | ✅ COMPLETED |
| **277** | #101 | crm | 1 | What is the policy for mileage decreases and VIN corrections? | Prefer survivorship/identity resolution with immutable external identifiers; avoid hard deletes; enforce normalization (E.164 phones, email); provide merge workflow with audit. | Is email unique globally, per legal entity, or per account? | ✅ COMPLETED |
| **276** | #96 | pricing | 1 | When multiple rules exist, is the combination logic AND or OR? | Prefer deterministic rule evaluation with explicit precedence (SKU>category>global); persist applied rule traces and snapshots; block finalization if required base data is missing unless override is permitted. | Are promotions stackable by default, and what are override thresholds? | ✅ COMPLETED |
|  |  |  | 2 | What are the contracts for fleet size and vehicle tag sources? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | Confirm the initial set of reasonCode enums required. | Use managed, stable reasonCode enums (configurable list with immutable codes) and require justification notes for privileged actions; audit who approved and when. | Which reason codes are mandatory for launch, and who owns maintaining the list? | ✅ COMPLETED |
| **275** | #94 | crm | 1 | Does this service enforce usage limits or only record redemptions? | Prefer survivorship/identity resolution with immutable external identifiers; avoid hard deletes; enforce normalization (E.164 phones, email); provide merge workflow with audit. | Is email unique globally, per legal entity, or per account? | ✅ COMPLETED |
| **274** | #93 | workexec | 1 | What is the specific contract for the alias resolution endpoint? | Prefer immutable snapshots at approval/invoice boundaries; changes after approval use revision/change-request workflows with explicit permissions and audit; integrate via events with idempotency. | Which actions are allowed after customer approval (reprice, substitute, add/remove lines)? | ✅ COMPLETED |
|  |  |  | 2 | What are the exact data types and formats for CRM IDs? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | Is it permissible for crmContactIds to be an empty list? | Allow null/empty only when a dedicated ‘anonymous/cash’ mode is explicitly flagged; otherwise enforce required identifiers; validate at API boundary with clear errors. | Do you prefer a real ‘Cash Customer’ record instead of null identifiers? | ✅ COMPLETED |
| **273** | #87 | location | 1 | Should this story be narrowed only to domain:location CRUD? | Prefer location as operational unit with effective-dated configs (hours, buffers, capacity); default new locations to INACTIVE until validation passes; cascade changes only via explicit admin action. | What validations must pass before a location can be activated? | ✅ COMPLETED |
|  |  |  | 2 | Where should the "Inactive location" staffing rule be enforced? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What are the business rules for parent location status cascades? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | Does a new location always default to ACTIVE status? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **272** | #85 | workexec | 1 | What is the definitive location validation policy for clock-ins? | Prefer immutable snapshots at approval/invoice boundaries; changes after approval use revision/change-request workflows with explicit permissions and audit; integrate via events with idempotency. | Which actions are allowed after customer approval (reprice, substitute, add/remove lines)? | ✅ COMPLETED |
| **271** | #71 | workexec | 1 | What is the definitive API contract for the HR availability service? | Prefer immutable snapshots at approval/invoice boundaries; changes after approval use revision/change-request workflows with explicit permissions and audit; integrate via events with idempotency. | Which actions are allowed after customer approval (reprice, substitute, add/remove lines)? | ✅ COMPLETED |
|  |  |  | 2 | What is the source of truth for "mobile travel blocks"? | Treat the producing domain as authoritative for business semantics; downstream domains store immutable snapshots needed for posting/audit and link back for drilldown. | Which domain should be the long-term owner for this data (if different from current)? | ✅ COMPLETED |
|  |  |  | 3 | How should the system behave if the HR system times out? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | What is the required time granularity for checks (e.g., 15 mins)? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **270** | #90 | people | 1 | What is the authoritative state for a disabled user? | Prefer effective-dated assignments and statuses; store times in UTC; enforce one primary assignment per scope; all adjustments are append-only with approval workflow and audit. | Payroll period definition and whether edits are allowed after approval? | ✅ COMPLETED |
|  |  |  | 2 | Is ending location assignments optional or mandatory? | Allow null/empty only when a dedicated ‘anonymous/cash’ mode is explicitly flagged; otherwise enforce required identifiers; validate at API boundary with clear errors. | Do you prefer a real ‘Cash Customer’ record instead of null identifiers? | ✅ COMPLETED |
|  |  |  | 3 | What is the retry policy for downstream command failures? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **269** | #89 | people | 1 | How are overlapping/conflicting role assignments handled? | Use deterministic survivor-based merge with field-level precedence rules and full audit; mark sources as MERGED (read-only) and redirect lookups; never hard-delete. | Do any sources have higher precedence (e.g., DMS import vs manual entry)? | ✅ COMPLETED |
|  |  |  | 2 | Are any roles inherently global or location-scoped? | Use explicit RBAC permissions (string scopes) decoupled from role names; support location/legal-entity scoping and effective-dated grants; audit all privileged actions. | Which roles exist at launch (e.g., SERVICE_ADVISOR, INVENTORY_MANAGER), and which are global vs location-scoped? | ✅ COMPLETED |
|  |  |  | 3 | Confirm revocation is handled via effectiveEndDate. | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **268** | #88 | people | 1 | What are the specific enumerated values for user status? | Prefer effective-dated assignments and statuses; store times in UTC; enforce one primary assignment per scope; all adjustments are append-only with approval workflow and audit. | Payroll period definition and whether edits are allowed after approval? | ✅ COMPLETED |
|  |  |  | 2 | What specific fields are required in contactInfo? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | Should the API hard-block duplicates with Conflict? | Require idempotencyKey on all writes/events; store (key, payloadHash, resultRef) with retention; duplicates return the original result (or 409 with DUPLICATE_EVENT code) and are audited. | Prefer ‘idempotent success’ vs ‘409 conflict’ for duplicates? | ✅ COMPLETED |
| **267** | #86 | people | 1 | What is the behavior for existing primary location assignments? | Prefer effective-dated assignments and statuses; store times in UTC; enforce one primary assignment per scope; all adjustments are append-only with approval workflow and audit. | Payroll period definition and whether edits are allowed after approval? | ✅ COMPLETED |
|  |  |  | 2 | Are effective dates full dates or timestamps (and what TZ)? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What is the precise, non-negotiable event schema? | Standardize on an idempotent event envelope: eventId(UUIDv7), eventType(enum), schemaVersion, occurredAt(UTC), producedBy, correlationId, idempotencyKey, payload(JSON). Version payload per eventType. | Target broker/transport (Kafka vs SNS/SQS/Rabbit) and required retention for replay? | ✅ COMPLETED |
|  |  |  | 4 | Can a person have multiple overlapping non-primary assignments? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **266** | #84 | people | 1 | Are there different break types (Meal, Rest) to record? | Prefer effective-dated assignments and statuses; store times in UTC; enforce one primary assignment per scope; all adjustments are append-only with approval workflow and audit. | Payroll period definition and whether edits are allowed after approval? | ✅ COMPLETED |
|  |  |  | 2 | What happens if a mechanic clocks out with an active break? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **265** | #83 | people | 1 | Define the workflow and permissions for "controlled adjustments." | Use explicit RBAC permissions (string scopes) decoupled from role names; support location/legal-entity scoping and effective-dated grants; audit all privileged actions. | Which roles exist at launch (e.g., SERVICE_ADVISOR, INVENTORY_MANAGER), and which are global vs location-scoped? | ✅ COMPLETED |
|  |  |  | 2 | What defines a "period" for time approval? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What is the workflow and state transition after a rejection? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **264** | #82 | workexec | 1 | What are the rules for allowing multiple concurrent timers? | Prefer immutable snapshots at approval/invoice boundaries; changes after approval use revision/change-request workflows with explicit permissions and audit; integrate via events with idempotency. | Which actions are allowed after customer approval (reprice, substitute, add/remove lines)? | ✅ COMPLETED |
|  |  |  | 2 | What is the behavior for "abandoned" timers at shift end? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | Is there a requirement to pause and resume a timer? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **263** | #81 | workexec | 1 | Which WorkOrder states prevent LaborPerformed entries? | Prefer immutable snapshots at approval/invoice boundaries; changes after approval use revision/change-request workflows with explicit permissions and audit; integrate via events with idempotency. | Which actions are allowed after customer approval (reprice, substitute, add/remove lines)? | ✅ COMPLETED |
|  |  |  | 2 | What is the retry policy for transient network/API failures? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **262** | #80 | people | 1 | Where is the "configurable threshold" for discrepancies managed? | Prefer effective-dated assignments and statuses; store times in UTC; enforce one primary assignment per scope; all adjustments are append-only with approval workflow and audit. | Payroll period definition and whether edits are allowed after approval? | ✅ COMPLETED |
| **261** | #78 | location | 1 | What are the definitions and scopes for check-in/cleanup buffers? | Prefer location as operational unit with effective-dated configs (hours, buffers, capacity); default new locations to INACTIVE until validation passes; cascade changes only via explicit admin action. | What validations must pass before a location can be activated? | ✅ COMPLETED |
| **260** | #77 | location | 1 | Are service/skill constraints free-text or foreign keys? | Prefer location as operational unit with effective-dated configs (hours, buffers, capacity); default new locations to INACTIVE until validation passes; cascade changes only via explicit admin action. | What validations must pass before a location can be activated? | ✅ COMPLETED |
|  |  |  | 2 | How should "capacity" be structured (Integer, Weight, etc.)? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What is the initial, definitive list of bay_type values? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **259** | #76 | location | 1 | What travel buffer policies (fixed vs dynamic) must be supported? | Prefer location as operational unit with effective-dated configs (hours, buffers, capacity); default new locations to INACTIVE until validation passes; cascade changes only via explicit admin action. | What validations must pass before a location can be activated? | ✅ COMPLETED |
|  |  |  | 2 | What is the source and model for "capability" tags? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | How are "service areas" defined (Zip, Polygon, etc.)? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | What is the integration contract for travel time to HR? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **258** | #74 | workexec | 1 | What is the specific performance SLA for the schedule endpoint? | Set explicit P95/P99 targets per endpoint; use caching for read-heavy views, avoid N+1 calls, and enforce request budgets with circuit breakers/timeouts; degrade gracefully for non-critical deps. | Expected peak QPS/concurrency and acceptable staleness windows for cached data? | ✅ COMPLETED |
|  |  |  | 2 | What are the precise rules for defining a "conflict"? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | Is the "HR availability overlay" a hard or soft dependency? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | What is the default time range for a "daily" view? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **257** | #72 | people | 1 | What is the integration pattern (Scheduled Pull vs Event Push)? | Prefer effective-dated assignments and statuses; store times in UTC; enforce one primary assignment per scope; all adjustments are append-only with approval workflow and audit. | Payroll period definition and whether edits are allowed after approval? | ✅ COMPLETED |
| **256** | #59 | workexec | 1 | What is the API for manager context overrides? | Prefer immutable snapshots at approval/invoice boundaries; changes after approval use revision/change-request workflows with explicit permissions and audit; integrate via events with idempotency. | Which actions are allowed after customer approval (reprice, substitute, add/remove lines)? | ✅ COMPLETED |
|  |  |  | 2 | What is the exact REST/event schema for OperationalContext? | Standardize on an idempotent event envelope: eventId(UUIDv7), eventType(enum), schemaVersion, occurredAt(UTC), producedBy, correlationId, idempotencyKey, payload(JSON). Version payload per eventType. | Target broker/transport (Kafka vs SNS/SQS/Rabbit) and required retention for replay? | ✅ COMPLETED |
|  |  |  | 3 | What specific event triggers the workorder context lock? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | What is the required egress mechanism for status reporting? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **255** | #54 | pricing | 1 | Do rules apply to SKUs, categories, or both (and precedence)? | Prefer deterministic rule evaluation with explicit precedence (SKU>category>global); persist applied rule traces and snapshots; block finalization if required base data is missing unless override is permitted. | Are promotions stackable by default, and what are override thresholds? | ✅ COMPLETED |
|  |  |  | 2 | What are the endpoints for Customer Tier and Cost/MSRP data? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | How should missing base cost/MSRP values be handled? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | Is there a single company-wide "Base" Price Book? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **254** | #52 | pricing | 1 | Who is the designated approver for threshold-exceeding overrides? | Prefer deterministic rule evaluation with explicit precedence (SKU>category>global); persist applied rule traces and snapshots; block finalization if required base data is missing unless override is permitted. | Are promotions stackable by default, and what are override thresholds? | ✅ COMPLETED |
|  |  |  | 2 | What is the notification and escalation path for approvals? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What is the lifecycle and notification process for rejections? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **253** | #70 | workexec | 1 | What are the roles, checks, and justifications for overrides? | Use explicit RBAC permissions (string scopes) decoupled from role names; support location/legal-entity scoping and effective-dated grants; audit all privileged actions. | Which roles exist at launch (e.g., SERVICE_ADVISOR, INVENTORY_MANAGER), and which are global vs location-scoped? | ✅ COMPLETED |
|  |  |  | 2 | How is collective skill possession defined for a job? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What is the required payload schema for assignment events? | Standardize on an idempotent event envelope: eventId(UUIDv7), eventType(enum), schemaVersion, occurredAt(UTC), producedBy, correlationId, idempotencyKey, payload(JSON). Version payload per eventType. | Target broker/transport (Kafka vs SNS/SQS/Rabbit) and required retention for replay? | ✅ COMPLETED |
| **252** | #67 | workexec | 1 | What is the definitive list of supported travel segment types? | Prefer immutable snapshots at approval/invoice boundaries; changes after approval use revision/change-request workflows with explicit permissions and audit; integrate via events with idempotency. | Which actions are allowed after customer approval (reprice, substitute, add/remove lines)? | ✅ COMPLETED |
|  |  |  | 2 | What are the specific auto-apply buffer policies? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What is the schema and transport for HR/Payroll integration? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | What are the conditions for editing segments on behalf of others? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **251** | #66 | workexec | 1 | What is the functional and data model for time adjustments? | Prefer immutable snapshots at approval/invoice boundaries; changes after approval use revision/change-request workflows with explicit permissions and audit; integrate via events with idempotency. | Which actions are allowed after customer approval (reprice, substitute, add/remove lines)? | ✅ COMPLETED |
|  |  |  | 2 | What are the specific criteria that define an "Exception"? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What are the specific fields and transport for HR integration? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **250** | #63 | workexec | 1 | What is the mapping table for Workexec to Appointment statuses? | Prefer immutable snapshots at approval/invoice boundaries; changes after approval use revision/change-request workflows with explicit permissions and audit; integrate via events with idempotency. | Which actions are allowed after customer approval (reprice, substitute, add/remove lines)? | ✅ COMPLETED |
|  |  |  | 2 | Is the reopenFlag permanent once set to true? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | How is the Appointment identified from the incoming event? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **249** | #61 | audit | 1 | Which domain is the primary owner for audit extensions? | Prefer asynchronous audit logging via outbox/event stream; store structured change summaries and correlationIds; enforce retention by category and protect access to sensitive fields. | Retention requirements for financial/security audit trails (years)? | ✅ COMPLETED |
|  |  |  | 2 | Is auditing a synchronous, critical-path dependency? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What is the data retention policy for audit records? | Make retention configurable by record category; financial documents and signatures should follow statutory/accounting retention (often multi-year) and must be immutable once archived. | Required retention by jurisdiction/customer contracts (e.g., 7 years for invoices/audit)? | ✅ COMPLETED |
|  |  |  | 4 | Is the "reason code" free-text or from a managed list? | Use managed, stable reasonCode enums (configurable list with immutable codes) and require justification notes for privileged actions; audit who approved and when. | Which reason codes are mandatory for launch, and who owns maintaining the list? |
|  |  |  | 5 | Is there a need to filter by date range or actor? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
|  |  |  | 6 | What is the structure for the ChangeSummary (Text vs JSON)? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **248** | #51 | pricing | 1 | What is the authoritative rounding policy for financial calculations? | Use currency-scale rounding (e.g., USD=2) with HALF_UP; round tax per line, then sum; persist explicit rounding deltas for audit/replay. | Any jurisdictions requiring HALF_EVEN (banker’s rounding) or cash-rounding increments (e.g., 0.05)? | ✅ COMPLETED |
|  |  |  | 2 | What is the specific P95 latency target for the pricing endpoint? | Set explicit P95/P99 targets per endpoint; use caching for read-heavy views, avoid N+1 calls, and enforce request budgets with circuit breakers/timeouts; degrade gracefully for non-critical deps. | Expected peak QPS/concurrency and acceptable staleness windows for cached data? | ✅ COMPLETED |
|  |  |  | 3 | Is a coupled Inventory availability check a hard requirement? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | What is the fallback behavior if no pricing rules are found? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **247** | #60 | workexec | 1 | What is the performance SLA for dashboard initial load? | Set explicit P95/P99 targets per endpoint; use caching for read-heavy views, avoid N+1 calls, and enforce request budgets with circuit breakers/timeouts; degrade gracefully for non-critical deps. | Expected peak QPS/concurrency and acceptable staleness windows for cached data? | ✅ COMPLETED |
|  |  |  | 2 | What is the required refresh rate (Real-time vs Polling)? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What is the contract for availability signals from the HR domain? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | What specific rules define a "Conflict" exception? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **245** | #55 | inventory | 1 | What is the authoritative list of lifecycle states? | Implement a finite state machine with enumerated states + allowed transitions; store transition audit (who/when/why); keep enums stable and extend via config only where safe. | Any must-have states/transitions for v1 beyond the proposed set? | ✅ COMPLETED |
|  |  |  | 2 | Which roles have permission to override discontinued blocks? | Use explicit RBAC permissions (string scopes) decoupled from role names; support location/legal-entity scoping and effective-dated grants; audit all privileged actions. | Which roles exist at launch (e.g., SERVICE_ADVISOR, INVENTORY_MANAGER), and which are global vs location-scoped? | ✅ COMPLETED |
|  |  |  | 3 | Can a product be replaced by more than one alternative? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | What is the granularity for effectiveStartDate (e.g., 00:00 UTC)? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **243** | #50 | pricing | 1 | Which domain (Pricing or Workexec) is primary for implementation? | Prefer deterministic rule evaluation with explicit precedence (SKU>category>global); persist applied rule traces and snapshots; block finalization if required base data is missing unless override is permitted. | Are promotions stackable by default, and what are override thresholds? | ✅ COMPLETED |
|  |  |  | 2 | Should the story be split into capability vs. integration? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | Is a hard-fail the desired behavior if a snapshot cannot be created? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | What are the specific requirements for snapshot drilldown? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **242** | #49 | workexec | 1 | What is the system behavior if the Pricing service is unavailable? | Prefer immutable snapshots at approval/invoice boundaries; changes after approval use revision/change-request workflows with explicit permissions and audit; integrate via events with idempotency. | Which actions are allowed after customer approval (reprice, substitute, add/remove lines)? | ✅ COMPLETED |
|  |  |  | 2 | What is the preferred data model for substitution traceability? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | Is the substitute price locked once applied to the line? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **241** | #48 | inventory | 1 | What is the authoritative formula for calculating ATP? | Prefer append-only inventory ledger movements (receipt/transfer/adjust/consume/return) with effective-dated rules; enforce lot/serial where required; emit events via outbox. | Do you need lot/serial tracking and FEFO rules in v1? | ✅ COMPLETED |
| **240** | #47 | inventory | 1 | What are the rules for normalizing lead time and region codes? | Prefer append-only inventory ledger movements (receipt/transfer/adjust/consume/return) with effective-dated rules; enforce lot/serial where required; emit events via outbox. | Do you need lot/serial tracking and FEFO rules in v1? | ✅ COMPLETED |
|  |  |  | 2 | What is the operational plan and SLA for the exception queue? | Set explicit P95/P99 targets per endpoint; use caching for read-heavy views, avoid N+1 calls, and enforce request budgets with circuit breakers/timeouts; degrade gracefully for non-critical deps. | Expected peak QPS/concurrency and acceptable staleness windows for cached data? | ✅ COMPLETED |
|  |  |  | 3 | Define the exact contract for the v1 stub connector. | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **239** | #46 | inventory | 1 | What system is the authoritative source for ManufacturerPartMap? | Treat the producing domain as authoritative for business semantics; downstream domains store immutable snapshots needed for posting/audit and link back for drilldown. | Which domain should be the long-term owner for this data (if different from current)? | ✅ COMPLETED |
|  |  |  | 2 | What is the expected data format (JSON/CSV) for feeds? | Use RFC4180 UTF-8 CSV with stable headers, explicit currency/amount columns, and cursor-based paging for large exports; protect via permissions and audit exports. | Do you need separate export formats (e.g., IIF/OFX) or only CSV initially? | ✅ COMPLETED |
|  |  |  | 3 | Define the scope of "min-order rules" (optional field vs story). | Allow null/empty only when a dedicated ‘anonymous/cash’ mode is explicitly flagged; otherwise enforce required identifiers; validate at API boundary with clear errors. | Do you prefer a real ‘Cash Customer’ record instead of null identifiers? | ✅ COMPLETED |
| **238** | #43 | product | 1 | Which domain is the System of Record for RestrictionRule entities? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 2 | What is the technical contract for synchronous enforcement? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | Should the system "fail open" or "fail closed" if rules are down? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | What specific location and service tags must be supported? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
|  |  |  | 5 | What is the expected backend-influenced user flow for overrides? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **237** | #42 | security | 1 | Which domain is primary for foundational security? | Prefer centralized permission catalog with scoped grants (global/legalEntity/location) and effective dates; enforce least privilege, audit protected operations, and standardize authN via OIDC/JWT + mTLS for S2S. | Which IdP/OIDC provider is standard for this program? | ✅ COMPLETED |
|  |  |  | 2 | Is the domain:resource:action permission format approved? | Use explicit RBAC permissions (string scopes) decoupled from role names; support location/legal-entity scoping and effective-dated grants; audit all privileged actions. | Which roles exist at launch (e.g., SERVICE_ADVISOR, INVENTORY_MANAGER), and which are global vs location-scoped? | ✅ COMPLETED |
|  |  |  | 3 | What is the mechanism for a domain to declare permissions? | Use explicit RBAC permissions (string scopes) decoupled from role names; support location/legal-entity scoping and effective-dated grants; audit all privileged actions. | Which roles exist at launch (e.g., SERVICE_ADVISOR, INVENTORY_MANAGER), and which are global vs location-scoped? | ✅ COMPLETED |
|  |  |  | 4 | Is the creation of business-oriented roles in scope? | Use explicit RBAC permissions (string scopes) decoupled from role names; support location/legal-entity scoping and effective-dated grants; audit all privileged actions. | Which roles exist at launch (e.g., SERVICE_ADVISOR, INVENTORY_MANAGER), and which are global vs location-scoped? |
|  |  |  | 5 | How does location factor into the permission model? | Use explicit RBAC permissions (string scopes) decoupled from role names; support location/legal-entity scoping and effective-dated grants; audit all privileged actions. | Which roles exist at launch (e.g., SERVICE_ADVISOR, INVENTORY_MANAGER), and which are global vs location-scoped? |
| **235** | #38 | inventory | 1 | Should the story be split into configuration vs execution? | Prefer append-only inventory ledger movements (receipt/transfer/adjust/consume/return) with effective-dated rules; enforce lot/serial where required; emit events via outbox. | Do you need lot/serial tracking and FEFO rules in v1? | ✅ COMPLETED |
|  |  |  | 2 | Can the same location be both Staging and Quarantine? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | Is the move-from-quarantine permission enforcement in scope? | Use explicit RBAC permissions (string scopes) decoupled from role names; support location/legal-entity scoping and effective-dated grants; audit all privileged actions. | Which roles exist at launch (e.g., SERVICE_ADVISOR, INVENTORY_MANAGER), and which are global vs location-scoped? | ✅ COMPLETED |
| **234** | #37 | inventory | 1 | What specific permissions or roles are required for ADJUST movements? | Use explicit RBAC permissions (string scopes) decoupled from role names; support location/legal-entity scoping and effective-dated grants; audit all privileged actions. | Which roles exist at launch (e.g., SERVICE_ADVISOR, INVENTORY_MANAGER), and which are global vs location-scoped? | ✅ COMPLETED |
| **233** | #36 | inventory | 1 | Confirm the official ATP formula (include expected receipts?). | Prefer append-only inventory ledger movements (receipt/transfer/adjust/consume/return) with effective-dated rules; enforce lot/serial where required; emit events via outbox. | Do you need lot/serial tracking and FEFO rules in v1? | ✅ COMPLETED |
|  |  |  | 2 | Define the scope of UOM handling (Base UOM vs multi-UOM). | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | Define a specific performance target for the API P95 latency. | Set explicit P95/P99 targets per endpoint; use caching for read-heavy views, avoid N+1 calls, and enforce request budgets with circuit breakers/timeouts; degrade gracefully for non-critical deps. | Expected peak QPS/concurrency and acceptable staleness windows for cached data? | ✅ COMPLETED |
|  |  |  | 4 | Which specific ledger event types sum to 'On-Hand'? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **232** | #35 | inventory | 1 | What is the primary method for providing PO/ASN identifiers? | Prefer append-only inventory ledger movements (receipt/transfer/adjust/consume/return) with effective-dated rules; enforce lot/serial where required; emit events via outbox. | Do you need lot/serial tracking and FEFO rules in v1? | ✅ COMPLETED |
|  |  |  | 2 | Is "blind" receiving (no source document) allowed or blocked? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | Is counting items and variances out of scope for this story? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **231** | #33 | inventory | 1 | Should receipt confirmation be fully automatic or manual? | Prefer append-only inventory ledger movements (receipt/transfer/adjust/consume/return) with effective-dated rules; enforce lot/serial where required; emit events via outbox. | Do you need lot/serial tracking and FEFO rules in v1? | ✅ COMPLETED |
|  |  |  | 2 | What is the schema and transport for Workexec notifications? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | How are mismatched part numbers handled during receipt? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **230** | #32 | inventory | 1 | What is the precedence order for overlapping put-away rules? | Prefer append-only inventory ledger movements (receipt/transfer/adjust/consume/return) with effective-dated rules; enforce lot/serial where required; emit events via outbox. | Do you need lot/serial tracking and FEFO rules in v1? | ✅ COMPLETED |
|  |  |  | 2 | Should single lines or consolidated lines drive task creation? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What is the mechanism for task assignment (Manual vs Pool)? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | What happens if a rule's suggested destination is full? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **229** | #31 | inventory | 1 | How are full/invalid destination storage scans handled? | Prefer append-only inventory ledger movements (receipt/transfer/adjust/consume/return) with effective-dated rules; enforce lot/serial where required; emit events via outbox. | Do you need lot/serial tracking and FEFO rules in v1? | ✅ COMPLETED |
|  |  |  | 2 | What happens if a "from" scan shows no quantity in data? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **228** | #30 | inventory | 1 | What is the trigger for replenishment (Real-time vs Batch)? | Prefer append-only inventory ledger movements (receipt/transfer/adjust/consume/return) with effective-dated rules; enforce lot/serial where required; emit events via outbox. | Do you need lot/serial tracking and FEFO rules in v1? | ✅ COMPLETED |
|  |  |  | 2 | What is the sourcing logic for multiple backstock locations? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **227** | #29 | inventory | 1 | What specific business logic defines "Soft" vs "Hard" allocation? | Prefer append-only inventory ledger movements (receipt/transfer/adjust/consume/return) with effective-dated rules; enforce lot/serial where required; emit events via outbox. | Do you need lot/serial tracking and FEFO rules in v1? | ✅ COMPLETED |
| **226** | #28 | inventory | 1 | What logic determines "priority" and "due time" for pick tasks? | Prefer append-only inventory ledger movements (receipt/transfer/adjust/consume/return) with effective-dated rules; enforce lot/serial where required; emit events via outbox. | Do you need lot/serial tracking and FEFO rules in v1? | ✅ COMPLETED |
|  |  |  | 2 | What defines the "route/location" sorting algorithm? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | How is a "suggested" location picked if stock is in multiple spots? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **225** | #27 | inventory | 1 | Clarify recount logic, triggers, and auditability processes. | Prefer append-only inventory ledger movements (receipt/transfer/adjust/consume/return) with effective-dated rules; enforce lot/serial where required; emit events via outbox. | Do you need lot/serial tracking and FEFO rules in v1? | ✅ COMPLETED |
| **224** | #26 | inventory | 1 | What are the criteria for approval thresholds (Cost, %, Units)? | Prefer append-only inventory ledger movements (receipt/transfer/adjust/consume/return) with effective-dated rules; enforce lot/serial where required; emit events via outbox. | Do you need lot/serial tracking and FEFO rules in v1? | ✅ COMPLETED |
|  |  |  | 2 | Are below-threshold adjustments automatically approved? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | Is a single level of approval sufficient? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | How are users with approval permissions notified? | Use explicit RBAC permissions (string scopes) decoupled from role names; support location/legal-entity scoping and effective-dated grants; audit all privileged actions. | Which roles exist at launch (e.g., SERVICE_ADVISOR, INVENTORY_MANAGER), and which are global vs location-scoped? |
| **223** | #25 | inventory | 1 | What is the decision hierarchy for presenting shortage options? | Prefer append-only inventory ledger movements (receipt/transfer/adjust/consume/return) with effective-dated rules; enforce lot/serial where required; emit events via outbox. | Do you need lot/serial tracking and FEFO rules in v1? | ✅ COMPLETED |
|  |  |  | 2 | What is the schema for fetching substitutes from Product? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What is the schema for fetching availability from Positivity? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | What is the specific timeout threshold for dependent calls? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
|  |  |  | 5 | Where is the estimatedLeadTimeDays sourced from? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **222** | #24 | inventory | 1 | What rules prevent "starvation" of lower priority orders? | Prefer append-only inventory ledger movements (receipt/transfer/adjust/consume/return) with effective-dated rules; enforce lot/serial where required; emit events via outbox. | Do you need lot/serial tracking and FEFO rules in v1? | ✅ COMPLETED |
|  |  |  | 2 | Is the Priority DESC, Due Time ASC sorting logic complete? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What is the enumerated list of audit reason codes? | Use managed, stable reasonCode enums (configurable list with immutable codes) and require justification notes for privileged actions; audit who approved and when. | Which reason codes are mandatory for launch, and who owns maintaining the list? | ✅ COMPLETED |
| **221** | #21 | workexec | 1 | What is the policy if Inventory reports insufficient stock? | Prefer immutable snapshots at approval/invoice boundaries; changes after approval use revision/change-request workflows with explicit permissions and audit; integrate via events with idempotency. | Which actions are allowed after customer approval (reprice, substitute, add/remove lines)? | ✅ COMPLETED |
|  |  |  | 2 | What is the precise behavior for linking estimates/workorders? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | Is an anonymous cart (no customerId) a valid use case? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | Is Pricing a hard dependency and what is the fallback? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **220** | #18 | workexec | 1 | Which domain (Order, Payment, Workexec) is primary for policy? | Prefer immutable snapshots at approval/invoice boundaries; changes after approval use revision/change-request workflows with explicit permissions and audit; integrate via events with idempotency. | Which actions are allowed after customer approval (reprice, substitute, add/remove lines)? | ✅ COMPLETED |
|  |  |  | 2 | Which work statuses specifically block cancellation? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What is the policy if a payment is already Captured and Settled? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | Is moving to CancellationFailed correct for downstream errors? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **219** | #17 | inventory | 1 | What is the specific, measurable target latency for search? | Set explicit P95/P99 targets per endpoint; use caching for read-heavy views, avoid N+1 calls, and enforce request budgets with circuit breakers/timeouts; degrade gracefully for non-critical deps. | Expected peak QPS/concurrency and acceptable staleness windows for cached data? | ✅ COMPLETED |
|  |  |  | 2 | What specific product fields should "keyword" match against? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What is the default and maximum allowable page size? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **218** | #16 | positivity | 1 | What is the partial failure policy for downstream services? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 2 | What is the source and format for external lead-time hints? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What is the response caching strategy and TTL? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
| **217** | #3 | crm | 1 | Who is the authoritative source for customer billing rules? | Treat the producing domain as authoritative for business semantics; downstream domains store immutable snapshots needed for posting/audit and link back for drilldown. | Which domain should be the long-term owner for this data (if different from current)? | ✅ COMPLETED |
|  |  |  | 2 | Is there a specific format or pattern required for PO numbers? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What is the override permission scope and approval workflow? | Use explicit RBAC permissions (string scopes) decoupled from role names; support location/legal-entity scoping and effective-dated grants; audit all privileged actions. | Which roles exist at launch (e.g., SERVICE_ADVISOR, INVENTORY_MANAGER), and which are global vs location-scoped? | ✅ COMPLETED |
|  |  |  | 4 | How do payment terms interact with PO requirements? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
|  |  |  | 5 | What is the default behavior for missing/misconfigured rules? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
| **216** | #2 | security | 1 | What is the complete list of protected operations and permissions? | Use explicit RBAC permissions (string scopes) decoupled from role names; support location/legal-entity scoping and effective-dated grants; audit all privileged actions. | Which roles exist at launch (e.g., SERVICE_ADVISOR, INVENTORY_MANAGER), and which are global vs location-scoped? | ✅ COMPLETED |
|  |  |  | 2 | Are roles flat or hierarchical with inheritance rules? | Use explicit RBAC permissions (string scopes) decoupled from role names; support location/legal-entity scoping and effective-dated grants; audit all privileged actions. | Which roles exist at launch (e.g., SERVICE_ADVISOR, INVENTORY_MANAGER), and which are global vs location-scoped? | ✅ COMPLETED |
|  |  |  | 3 | Is the HR system the authoritative source for user identity? | Treat the producing domain as authoritative for business semantics; downstream domains store immutable snapshots needed for posting/audit and link back for drilldown. | Which domain should be the long-term owner for this data (if different from current)? | ✅ COMPLETED |
|  |  |  | 4 | Are permissions scoped globally, per-location, or per-shop? | Use explicit RBAC permissions (string scopes) decoupled from role names; support location/legal-entity scoping and effective-dated grants; audit all privileged actions. | Which roles exist at launch (e.g., SERVICE_ADVISOR, INVENTORY_MANAGER), and which are global vs location-scoped? |
|  |  |  | 5 | Can roles be assigned with an expiration date? | Use explicit RBAC permissions (string scopes) decoupled from role names; support location/legal-entity scoping and effective-dated grants; audit all privileged actions. | Which roles exist at launch (e.g., SERVICE_ADVISOR, INVENTORY_MANAGER), and which are global vs location-scoped? |
| **215** | #1 | accounting | 1 | Who decides the timing and responsibility of GL postings? | Prefer double-entry journal posting from immutable source events; enforce debits=credits, period controls, and traceability to source documents; never mutate posted journals—use reversals/adjustments. | Single legal entity vs multi-entity at launch? | ✅ COMPLETED |
|  |  |  | 2 | What are the override authority and threshold rules? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 3 | What refund scenarios (reversal, memo, adj) are in scope? | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? | ✅ COMPLETED |
|  |  |  | 4 | Define the cancellation scope and associated GL impacts. | Apply standard best practices: explicit contracts, idempotency, append-only audit trails, UTC timestamps, scoped RBAC, and configurable policy defaults. | Any domain-specific constraints not captured in the story? |
