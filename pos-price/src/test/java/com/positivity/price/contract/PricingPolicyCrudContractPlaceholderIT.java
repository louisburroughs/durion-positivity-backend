package com.positivity.price.contract;

import com.positivity.price.BaseContractIntegrationTest;
import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

/**
 * Placeholder contract suite for future Pricing Policy CRUD API.
 *
 * Planned endpoints:
 * - POST /v1/prices
 * - GET /v1/prices/{id}
 * - PATCH /v1/prices/{id}
 */
@Disabled("Future placeholder: Pricing Policy CRUD (/v1/prices) is not implemented in pos-price yet.")
@DisplayName("Pricing Policy CRUD Contract Behavior (Future Placeholder)")
class PricingPolicyCrudContractPlaceholderIT extends BaseContractIntegrationTest {

    @Test
    @DisplayName("Placeholder: re-enable when PricingPolicyController is implemented")
    void placeholder() {
        // Intentionally empty.
    }
}
