package com.positivity.shopManager.repository;

import com.positivity.shopManager.entity.ShopQualification;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ShopQualificationRepository extends JpaRepository<ShopQualification, Long> {
}