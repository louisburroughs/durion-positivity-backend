# pos-shop-manager: tables with no tenant_id and no row-level-security policy (ADR-0062 section 5).
# Format: <table>  # <reason>. Every other table in this module is tenant-scoped.
# TenancySchemaConformanceIT (plan WS1) reads this file.
processed_events  # consumer idempotency ledger keyed by eventId, deduplicated across tenants; tenant_id carried as data for per-tenant reconciliation
conflict_rule  # DECISION-SHOPMGMT-002 rule catalog (CAP-326, spec D18.2): platform reference data whose code is the API reason code, one namespace for every tenant; a tenant must not be able to switch off a rule the exclusion constraint still enforces
