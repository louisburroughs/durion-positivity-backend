openapi: 3.0.3
info:
  title: Inventory API Catalog
  version: 0.1.0
  description: Catalog for inventory service APIs. - https://github.com/louisburroughs/durion/issues/213
  contact: {email: louis.burroughs@gmail.com}
servers:
  - url: http://localhost:8084
tags:
  - name: Inventory
    description: Operations related to inventory management
paths:
  /v1/inventory/locations/{locationId}/deactivate:
    post:
      description: Deactivate a storage location with optional stock transfer to another location. Contract stub for deactivating a storage location with atomic stock transfer (Option B). See story #39.
      tags: [Inventory]
      summary: Deactivate a storage location
      operationId: deactivateLocation
      security:
        - bearerAuth: []
      parameters:
        - name: locationId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: '#/components/schemas/DeactivateLocationRequest' }
      responses:
        '202': { description: Accepted }
        '400': { description: Bad Request }
        '404': { description: Not Found }
        '409': { description: Conflict }
components:
  securitySchemes:
    bearerAuth: { type: http, scheme: bearer, bearerFormat: JWT }
  schemas:
    DeactivateLocationRequest:
      type: object
      properties:
        destinationLocationId:
          type: string
          format: uuid
