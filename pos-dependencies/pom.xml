<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.positivity</groupId>
    <artifactId>pos-dependencies</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>pos-dependencies</name>
    <description>BOM for internal Positivity microservice artifacts and shared dependencies</description>

    <properties>
        <spotless-maven-plugin.version>2.43.0</spotless-maven-plugin.version>
        <palantir-java-format.version>2.90.0</palantir-java-format.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Shared Security Library (Gateway Authentication) -->
            <dependency>
                <groupId>com.positivity</groupId>
                <artifactId>pos-security-common</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Shared Event Library -->
            <dependency>
                <groupId>com.positivity</groupId>
                <artifactId>pos-events</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Shared Document Helper Library -->
            <dependency>
                <groupId>com.positivity</groupId>
                <artifactId>pos-document-helper</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Shared DTO Library -->
            <dependency>
                <groupId>com.positivity</groupId>
                <artifactId>pos-shared-dtos</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Shared Tax DTO and Validation Library -->
            <dependency>
                <groupId>com.positivity</groupId>
                <artifactId>pos-tax-common</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Test Aggregator (Architecture Testing) -->
            <dependency>
                <groupId>com.positivity</groupId>
                <artifactId>pos-archunit</artifactId>
                <version>${project.version}</version>
                <scope>test</scope>
            </dependency>

            <!-- Agent Framework (Shared Library) -->
            <dependency>
                <groupId>com.positivity</groupId>
                <artifactId>pos-agent-framework</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- UUID v7 Generator (ADR-0013) -->
            <dependency>
                <groupId>com.github.f4b6a3</groupId>
                <artifactId>uuid-creator</artifactId>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>${spotless-maven-plugin.version}</version>
                <configuration>
                    <java>
                        <palantirJavaFormat>
                            <version>${palantir-java-format.version}</version>
                        </palantirJavaFormat>
                    </java>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
