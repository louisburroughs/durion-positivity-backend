package com.positivity.warranty.internal.enums;

/** Human adjudication decision recorded on the claim (PRD §3.4, §8 decision endpoint). */
public enum ClaimDecision {
    APPROVE,
    DENY,
    REQUEST_INFO
}
