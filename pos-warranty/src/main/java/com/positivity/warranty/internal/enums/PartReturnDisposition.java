package com.positivity.warranty.internal.enums;

/** What happens to the defective part (PRD §3.8). */
public enum PartReturnDisposition {
    HOLD_FOR_INSPECTION,
    RETURN_TO_VENDOR,
    SCRAP_AUTHORIZED,
    CUSTOMER_RETAINED
}
