package com.positivity.warranty.internal.enums;

/** Per-line adjudication outcome (PRD §3.5 {@code lineDisposition}). */
public enum LineDisposition {
    PENDING,
    APPROVED,
    PARTIALLY_APPROVED,
    DENIED,
    WITHDRAWN
}
