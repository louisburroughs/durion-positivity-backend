package com.positivity.warranty.internal.enums;

/** How the credit fraction is computed per claim line (PRD §3.2, §6). */
public enum ProrationMethod {
    NONE,
    TREAD_DEPTH,
    MILEAGE,
    TIME
}
