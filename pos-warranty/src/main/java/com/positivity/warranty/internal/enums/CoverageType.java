package com.positivity.warranty.internal.enums;

/** Coverage type of a policy — mirrors the four claim types (PRD §3.2). */
public enum CoverageType {
    MANUFACTURER_DEFECT,
    DEALER_WORKMANSHIP,
    ROAD_HAZARD,
    EXTENDED_PLAN
}
