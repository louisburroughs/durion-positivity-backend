package com.positivity.tenant.internal.enums;

/** Role of a contact on an account (ADR-0062 §7); an account may carry several of each. */
public enum ContactRole {
    OWNER,
    BILLING,
    TECHNICAL
}
