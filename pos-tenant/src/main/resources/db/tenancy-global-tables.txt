# pos-tenant: tables with no tenant_id and no row-level-security policy (ADR-0062 section 5).
# Format: <table>  # <reason>. Every other table in this module is tenant-scoped (to the platform tenant).
# TenancySchemaConformanceIT reads this file.
event_outbox  # transactional outbox; the unbound poller publishes every row (tenant_id carried as data)
processed_events  # consumer idempotency ledger keyed by eventId; checked before the tenant is bound
