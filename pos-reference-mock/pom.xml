<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.positivity</groupId>
        <artifactId>positivity</artifactId>
        <version>${revision}</version>
    </parent>
    <artifactId>pos-reference-mock</artifactId>
    <name>pos-reference-mock</name>
    <description>Fake external labor-guide vendor serving the Durion-normalized provider contract from
        checked-in JSON fixtures (pos-catalog/docs/service-time-sourcing-plan.md §10, issue #1569
        Phase 1). Deliberately OUTSIDE the platform mesh: no Eureka registration, no gateway route,
        no JWT/security, no database, no Kafka, no pos-events. Reached only via adapter base-url
        config on fixed port 8095. Its OpenAPI spec is the normative provider contract that Phase-2
        vendor adapters translate onto.</description>

    <!-- pos-catalog/docs/service-time-sourcing-plan.md §10 keeps this contract-test double out
         of any coverage threshold derived from the aggregate report. It still carries its own
         floors below: the module is over the 50-line threshold at which
         docs/TEST_COVERAGE_IMPROVEMENT_PLAN.md §6.5 requires one, and the fixtures and vendor
         error shapes it serves are the normative provider contract — exactly the thing §10 wants
         kept from rotting. -->

    <packaging>jar</packaging>
    <properties>
        <!-- Coverage ratchet: measured 87.6% line / 79.2% branch by the gate's own
             command, `verify -DskipITs` (docs/TEST_COVERAGE_IMPROVEMENT_PLAN.md §6.1).
             Floors sit 3 points under. Re-derive them ONLY from a -DskipITs run — an
             IT-inclusive measurement describes coverage the gate cannot see.
             Regenerate with scripts/update-coverage-floors.sh. -->
        <jacoco.line.min>0.84</jacoco.line.min>
        <jacoco.branch.min>0.76</jacoco.branch.min>
    </properties>

    <dependencies>
        <!-- Lombok for boilerplate code reduction -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>provided</scope>
        </dependency>
        <!-- JSpecify for null safety annotations -->
        <dependency>
            <groupId>org.jspecify</groupId>
            <artifactId>jspecify</artifactId>
            <version>1.0.0</version>
        </dependency>
        <!-- Spring Web for the mock vendor REST endpoints -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <!-- Actuator + Prometheus: the deploy wiring (check-deploy-service-drift.sh) requires a
             scrape job for every containerized service; the mock stays outside the JWT boundary,
             so its metrics endpoint is unauthenticated on the compose network. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!-- OpenAPI: this module's spec is the normative Durion-normalized provider contract -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc-openapi.version}</version>
        </dependency>
        <!-- Kotlin Reflect - required by springdoc-openapi 3.x -->
        <dependency>
            <groupId>org.jetbrains.kotlin</groupId>
            <artifactId>kotlin-reflect</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <skip>false</skip>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
